import styled from "styled-components";

const Button = styled("button")`
	background-color: transparent;
	border: none;
	cursor: pointer;
	display: flex;
	font-size: 1em;
	padding: 0;
`;

export { Button };
